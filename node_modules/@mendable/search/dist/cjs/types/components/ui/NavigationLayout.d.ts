import React from "react";
import { MendableSearchStyle } from "../types";
export default function NavigationLayout({ children, resetButton, topMessage, onBackClick, setIsPinnedButton, style, }: {
    children: React.ReactNode;
    topMessage: string;
    resetButton: React.ReactNode;
    setIsPinnedButton: React.ReactNode;
    onBackClick?: () => void;
    style: MendableSearchStyle;
}): React.JSX.Element;
