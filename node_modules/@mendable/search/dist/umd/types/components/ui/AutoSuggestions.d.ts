import React from 'react';
export default function AutoSuggestions({ darkMode, questions, onClick }: {
    darkMode: boolean;
    questions: string[];
    onClick: (question: string) => any;
}): React.JSX.Element;
export declare function AutoSuggestionsMini({ darkMode, questions, onClick }: {
    darkMode: boolean;
    questions: string[];
    onClick: (question: string) => any;
}): React.JSX.Element;
