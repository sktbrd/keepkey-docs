import React from "react";
import { Alert, MendableSearchStyle, MessageSettings } from "../types";
interface Props {
    message: string;
    message_id: number | null;
    sources: Array<any>;
    isBot: boolean;
    messageStyle: MendableSearchStyle;
    sourcesStyle: MendableSearchStyle;
    initialMessage?: boolean;
    alert?: Alert;
    alertMessage?: string;
    botIcon?: JSX.Element | React.ReactNode | string | undefined;
    userIcon?: JSX.Element | React.ReactNode | string | undefined;
    messageSettings?: MessageSettings;
    isGenerating?: boolean;
}
export declare const Message: ({ message, message_id, messageStyle, sourcesStyle, isBot, sources, alert, alertMessage, messageSettings, botIcon, userIcon, isGenerating, initialMessage, }: Props) => React.JSX.Element;
export {};
